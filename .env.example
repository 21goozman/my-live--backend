# copy to .env and edit if needed
PORT=8080
CORS_ORIGINS=http://localhost:5500,http://127.0.0.1:5500
TIMEZONE=Africa/Nairobi

# Optional notifications (leave empty to disable)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
FROM_EMAIL=bookings@campusnails.local

# Optional SMS (stubbed by default)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM=
